const Proposal = require("../models/Proposal"); const Job = require("../models/Job"); const User = require("../models/User"); const { id, now, pick } = require("../utils/helpers");
function list(q,user){let a=Proposal.all();if(q.jobId)a=a.filter(x=>x.jobId===q.jobId);if(q.freelancerId)a=a.filter(x=>x.freelancerId===q.freelancerId);if(q.status)a=a.filter(x=>x.status===q.status);if(user.role==="freelancer")a=a.filter(x=>x.freelancerId===user.id);return a.map(proposal=>{const freelancer=User.findById(proposal.freelancerId); return{...proposal, freelancer:freelancer?{id:freelancer.id,name:freelancer.name,email:freelancer.email}:null};});}
function get(i){const x=Proposal.findById(i);if(!x)throw Object.assign(new Error("Proposal not found"),{status:404});return x;}
function create(b,userId){const job=Job.findById(b.jobId);if(!job)throw Object.assign(new Error("Job not found"),{status:404});if(job.clientId===userId)throw Object.assign(new Error("You cannot apply to your own job"),{status:400});if(Proposal.all().some(x=>x.jobId===job.id&&x.freelancerId===userId))throw Object.assign(new Error("You already submitted a proposal for this job"),{status:409});if(b.bid===undefined||b.bid===null||b.bid===""){throw Object.assign(new Error("Proposed Rate is required"),{status:400});}return Proposal.create({id:id(),jobId:job.id,freelancerId:userId,bid:Number(b.bid),estimatedCompletionTime:b.estimatedCompletionTime||"",coverLetter:b.coverLetter||"",skills:b.skills||[],status:"pending",createdAt:now(),updatedAt:now()});}
function update(i,b,user){const p=get(i),job=Job.findById(p.jobId);const isFreelancer=p.freelancerId===user.id,isClient=job&&job.clientId===user.id;if(!isFreelancer&&!isClient)throw Object.assign(new Error("Not allowed"),{status:403});const patch=isFreelancer?pick(b,["bid","estimatedCompletionTime","coverLetter","skills"]):pick(b,["status"]);return Proposal.update(i,{...patch,updatedAt:now()});}
function remove(i,userId){const p=get(i);if(p.freelancerId!==userId)throw Object.assign(new Error("Not allowed"),{status:403});Proposal.delete(i);}
module.exports={list,get,create,update,remove};
